app:
  scheduler:
    enabled: false
    parallelism: 1
  bot:
    base-url: ${wiremock.server.baseUrl}
    transport: http
  stackoverflow:
    base-url: ${wiremock.server.baseUrl}
    site: stackoverflow
    key: ""
    access-token: ""
  github:
    base-url: ${wiremock.server.baseUrl}
    token: ""
