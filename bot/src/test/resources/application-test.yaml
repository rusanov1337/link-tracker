app:
  telegram:
    url: ${wiremock.server.baseUrl}/bot
    token: test-token
    update-listener-sleep: 1s
    debug: true
    polling-enabled: false
    set-my-commands-enabled: false
  scrapper:
    base-url: ${wiremock.server.baseUrl}
